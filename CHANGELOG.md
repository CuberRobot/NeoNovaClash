# 变更记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号`。

## [v0.4.0] - 2026-09-19

一轮完整的平衡调整 + 抽池机制重做 + 浏览器实测发现的交互问题修复。

### 新增

- **平衡对拍工具（`tools/balance.py`）**：跑 N 场随机对局，输出每个角色的抽到率、出战率、
  出战胜率，以及标签胜率与先手优势，并直接标出偏强（>55%）与偏弱（<45%）的角色；
- **抽池机制重做**：从「双方各自独立抽 6 张」改为「共用抽 12 张后对半切」——
  双方不重复、每池至少 2 名带标签角色、同标签最多 2 名，并在 462 种切分里挑强度最接近的一种；
- **角色差异性测试**：禁止出现重复面板/重复三元组，并要求五名无标签角色保持
  「攻击递减、生命递增」的连续曲线，防止以后越调越像。

### 平衡调整（8000 场对拍，目标 45%~55%）

- 死灵法师：复活 2 次 / 40% → **1 次 / 30%**（调整前胜率 79%，一家独大）；
- 重装盔甲：减伤 60% → **70%**（重装系整体偏强）；
- 自爆步兵：击杀线 26 → **30**、攻击 16 → **18**，并新增「优先炸可击杀的高攻目标」的战术选择（调整前 41%）；
- 护盾部署者：攻击 2 → **3**、护盾 3 → **4** 次（调整前 33%）；
- 中毒：每层每轮 3 → **2** 点（毒药投手偏强）；
- 其余角色按数据逐个微调 ATK/HP/先手值（强角色先手值调高、弱角色调低）。

结果：20 名角色**全部落在 45%~55%**，A/B 胜率 3975:3999，平均 6.7 回合、最长 16 回合，平局率 0.3%。

### 修复（浏览器实测发现）

- **战斗回放会吃掉下一局的备战时间**：回放改成按真实时间推进（整场约 11 秒内播完），
  并在战报与结算卡片上显示「下一局剩余 N 秒」；后台标签页被浏览器限流时也会自动追赶；
- **对局结束后「再来一局」进不了新对局**：结算时清理上一场的残留状态，
  并保证任何 `round_start` 都能进入新局（附服务端回归测试）；
- **发版后浏览器仍用旧的 JS/CSS**：index.html 的资源 URL 由后端注入版本号（`?v=版本号`），
  `/static/*` 一律 `no-cache, must-revalidate`，Cloudflare 边缘也会跟着回源校验。

## [v0.3.0] - 2026-09-19

直接把内容铺到旧项目 v1.2 的规模：**20 名角色 / 12 种标签**，平衡性调整留到拿到对拍数据之后。

### 内容扩展

- **角色池扩到 20 名**：新增均衡战士D/E、暗影猎手、血怒斗士、晶壁守卫、噬魂者、荆棘守卫、处决者，
  数值与先手值沿用旧项目 v1.2 的定稿数据；
- **3 个扩展标签**：
  - 吸血：本次实际伤害的 25% 回复自身，单次上限 5，自爆与追击伤害不触发；
  - 反弹：本次实际损失生命的 40% 反弹给攻击者，不经过护盾、不受重装减免、不会被再次反弹，自爆伤害不参与；
  - 斩杀：目标当前生命 ≤ 最大生命的 25% 时，本次伤害 ×1.5；
- **引擎新增「受击后」结算阶段**：`TagRuntime.on_damaged` 与 `Battle.apply_raw_damage()`，
  反弹走独立的原始伤害通道（`HitContext.reflected`），保证不会出现递归反弹；
- **测试扩到 57 项**：每个新标签都有触发 / 不触发 / 上限 / 交互用例，另有卡池完整性检查；
- **随机对拍体检**：400 场无平局、无死循环（平均 6.2 回合、最长 15 回合），12 种标签全部实际出场。

### 工程与部署

- **部署模板（`deploy/`）**：systemd 服务单元、nginx 反向代理（含 WebSocket 升级头）、一键更新脚本；
- **文档**：`docs/宝塔面板部署.md`（宝塔 + systemd + nginx 全流程）、`deploy/cloudflared-config.yml`
  （Cloudflare Tunnel 方案），均不含任何服务器凭据；
- **线上 demo**：<https://novaclash.cc/>，通过 Cloudflare Tunnel 提供服务，
  源站不需要对外暴露 80/443。

### 修复

- 部署文档明确必须用 `python run.py` 启动：uvicorn 命令行不读取 `NC_HOST`/`NC_PORT`，
  会默认监听 8000，导致 nginx 反代 502。

## [v0.1.0] - 2026-09-18

第一个可玩的重建版本：从「手写碎片 + 两次失败实现」中抽离规则，重新搭好地基。

### 新增

- **游戏核心（`backend/core`）**
  - 12 名基础角色数据与 9 种标签的效果实现；
  - 战斗引擎：固定结算顺序、整数取整、随机种子可复现、事件化战报；
  - 规则层：角色池抽取、出战方案校验（选角数量、重复、自爆首位、增益次数与上限、策略合法性）；
  - 房间状态机：等待 → 备战 → 战斗 → 结算 → 结束，支持超时自动提交与平局重开。
- **Web 层（`backend/web`）**
  - FastAPI + WebSocket 房间制服务：创建房间、房间号加入、私有角色池下发、逐局结算；
  - 备战倒计时、断线关闭房间、空闲房间回收、房间数上限保护；
  - HTTP 接口：`/api/health`、`/api/version`、`/api/rules`。
- **前端（`frontend`）**
  - 原生 HTML/CSS/JS 单页应用，零构建步骤；
  - 大厅 → 等待 → 备战 → 战斗 → 结算完整流程，含实时校验提示、增益计数、倒计时、
    战斗回放（暂停 / 倍速 / 跳到结果）、比分与逐局历史、重连提示；
  - 规则页数据全部来自后端 `/api/rules`，不会与游戏数值脱节。
- **工具与测试**
  - `tools/cli.py`：命令行热座对战与随机对拍；
  - `tools/smoke_pvp.py`：两个机器人客户端在真实服务上跑完整场对局；
  - 46 项测试覆盖标签效果、规则校验、HTTP 接口与 WebSocket 全流程。
- **工程化**
  - `.gitignore` / `.editorconfig` / `.env.example` / `requirements*.txt` / `pytest.ini` / `pyproject.toml`（ruff 配置）；
  - `Dockerfile` 与 `docker-compose.yml`；
  - 文档：规则设定、架构设计、通信协议、部署指南、开发流程与版本管理、路线图。

### 说明

- 数值裁定：以旧项目设定文档为准（自爆阈值 26、复活 40%、重装 ≥7 减伤至 60%、
  护盾 50% 分担 3 次、中毒每层 3 点持续 3 轮最多 3 层），冲突处已在
  [docs/规则设定.md](docs/规则设定.md) 的「数值裁定」中记录理由。
- 本轮只实现标准模式；8 名扩展角色、吸血/反弹/斩杀、混沌模式与大战场模式列入路线图。

[v0.1.0]: https://github.com/CuberRobot/NeoNovaClash/releases/tag/v0.1.0
