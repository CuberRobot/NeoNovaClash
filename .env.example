# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略）
# 服务监听地址，容器部署时保持 0.0.0.0
NC_HOST=0.0.0.0
NC_PORT=8000

# 准备阶段限时（秒），超时自动随机提交
NC_PREPARE_TIMEOUT=60

# 房间在无人行动后自动回收的时间（秒）
NC_ROOM_TTL=1800

# 单进程允许的最大房间数，防止内存被占满
NC_MAX_ROOMS=500

# uvicorn 日志级别：critical / error / warning / info / debug
NC_LOG_LEVEL=info
