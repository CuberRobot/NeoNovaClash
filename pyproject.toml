[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = ["legacy", "venv", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = [
    "E501",  # 行长度由格式化工具把握，注释与文档允许长行
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
